// we want to maintain our own Router history instead of using BrowserRouter to make programatically routing easier.

import { createBrowserHistory } from 'history';
export default createBrowserHistory();